apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: flux-system
data:
  NFS_SERVER: "192.168.1.100"  # Replace with your Synology NAS IP
  NFS_PATH: "/volume1/kubernetes"  # Replace with your NFS export path
