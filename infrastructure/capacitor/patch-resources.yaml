apiVersion: apps/v1
kind: Deployment
metadata:
  name: capacitor
  namespace: flux-system
spec:
  template:
    spec:
      containers:
      - name: capacitor
        resources:
          requests:
            cpu: 50m
            memory: 200Mi
