# Add these secrets to your clusters/homelab/cluster-secrets.yaml file:
# 
# WGER_DOMAIN_NAME: wger.yourdomain.com
# WGER_DJANGO_SECRET_KEY: your-very-long-random-secret-key-here
#
# To generate a Django secret key, you can use:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
#
# Or use this command:
# openssl rand -base64 50
#
# Note: This configuration now reuses your existing postgres-config secret
# for database credentials, so no additional database passwords are needed!
